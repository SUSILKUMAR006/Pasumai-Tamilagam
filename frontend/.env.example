# Backend API origin — no trailing slash, no /api suffix.
# Local dev default (matches backend/.env PORT):
VITE_API_BASE_URL=http://localhost:5000

# Production example (Render backend service URL):
# VITE_API_BASE_URL=https://pasumai-tamilagam-backend.onrender.com
